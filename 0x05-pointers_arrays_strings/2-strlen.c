#include "main.h"
#include <string.h>

/**
 * _strlen - string length
 * @s: string
 *
 *
 * Return: 0 success
 */
int _strlen(char *s)
{
	char *s;

	printf("length of string is: %zu\n", strlen(*s));
	return (0);
}
