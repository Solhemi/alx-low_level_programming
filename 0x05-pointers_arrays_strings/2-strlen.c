#include "main.h"

/**
 * _strlen - string length
 * @s: string
 *
 *
 * Return: 0 success
 */
int _strlen(char *s)
{
	char *str[];
	int len;

	for (len = 0; *str[len] != '0'; ++len)
	{
		printf("length of string is: %d\n", len);
	}
	return (0);
}
