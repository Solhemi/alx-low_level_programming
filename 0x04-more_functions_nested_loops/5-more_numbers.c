#include "main.h"

/**
 * more_numbers - zero to 14
 *
 * Description: prints row 3 times
 *
 * Return: void
 */

void more_numbers(void)

{
	int x, y;

	if (y >= 0 && y <= 10)
	{
		for (x >= 0; x <= 14; x++)
		{
			_putchar(x + '0');
			_putchar('\n');
		}
		{
			_putchar('\n');
		}
	}
}
