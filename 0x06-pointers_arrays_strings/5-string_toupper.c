#include "main.h"

/**
 * string_toupper - f
 * 'a': char
 * 'n': array
 * 'z': char
 * @ ' ': char
 * Return: n
 */

char *string_toupper(char *)

{
	int j;

	j = 0;
	while (n[j] != '\0')
	{
		if (n[j] >= 'a' && n[j] <= 'z')
			n[j] = n[j] - 32;
		j++
	}
	return (n);
}
